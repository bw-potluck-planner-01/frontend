const initialState = {
  username: "",
  password: "",
  token: null,
};

export default function Reducer(state = initialState, action) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
